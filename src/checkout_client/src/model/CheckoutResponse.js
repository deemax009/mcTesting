/**
 * Checkout, Save Card On File, Delete Saved Card On File And Confirmations API
 *  Click to Pay checkout used by Merchants to receive payload data from the Click to Pay System and send the results of transaction authorisation back to the Click to Pay System. Endpoints are available to retrieve payload data and send the outcome of a payment or checkout. 
 *
 * The version of the OpenAPI document: 1.3.2
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssuranceData from './AssuranceData';
import CheckoutEncryptedPayload from './CheckoutEncryptedPayload';
import CheckoutMaskedCard from './CheckoutMaskedCard';
import CheckoutMaskedConsumer from './CheckoutMaskedConsumer';

/**
 * The CheckoutResponse model module.
 * @module model/CheckoutResponse
 * @version 1.3.2
 */
class CheckoutResponse {
    /**
     * Constructs a new <code>CheckoutResponse</code>.
     * @alias module:model/CheckoutResponse
     * @param merchantTransactionId {String} Merchant Checkout Transaction Identifier which is returned in the checkoutResponse to either checkoutWithCard or checkoutWithNewCard JavaScript methods and links the client-side JavaScript calls and server-side API calls for a specific transaction. Optional for Subsequent Payment or Subsequent Split Shipment.
     * @param encryptedPayload {module:model/CheckoutEncryptedPayload} 
     * @param correlationId {String} Correlation ID for this transaction. If available within the present checkout session (e.g. received in an earlier API response during the present session), then it must be provided, otherwise a new checkout session will be initiated.
     */
    constructor(merchantTransactionId, encryptedPayload, correlationId) { 
        
        CheckoutResponse.initialize(this, merchantTransactionId, encryptedPayload, correlationId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, merchantTransactionId, encryptedPayload, correlationId) { 
        obj['merchantTransactionId'] = merchantTransactionId;
        obj['encryptedPayload'] = encryptedPayload;
        obj['correlationId'] = correlationId;
    }

    /**
     * Constructs a <code>CheckoutResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CheckoutResponse} obj Optional instance to populate.
     * @return {module:model/CheckoutResponse} The populated <code>CheckoutResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CheckoutResponse();

            if (data.hasOwnProperty('merchantTransactionId')) {
                obj['merchantTransactionId'] = ApiClient.convertToType(data['merchantTransactionId'], 'String');
            }
            if (data.hasOwnProperty('encryptedPayload')) {
                obj['encryptedPayload'] = CheckoutEncryptedPayload.constructFromObject(data['encryptedPayload']);
            }
            if (data.hasOwnProperty('correlationId')) {
                obj['correlationId'] = ApiClient.convertToType(data['correlationId'], 'String');
            }
            if (data.hasOwnProperty('merchantCardId')) {
                obj['merchantCardId'] = ApiClient.convertToType(data['merchantCardId'], 'String');
            }
            if (data.hasOwnProperty('assuranceData')) {
                obj['assuranceData'] = AssuranceData.constructFromObject(data['assuranceData']);
            }
            if (data.hasOwnProperty('maskedCard')) {
                obj['maskedCard'] = CheckoutMaskedCard.constructFromObject(data['maskedCard']);
            }
            if (data.hasOwnProperty('maskedConsumer')) {
                obj['maskedConsumer'] = CheckoutMaskedConsumer.constructFromObject(data['maskedConsumer']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CheckoutResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CheckoutResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CheckoutResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['merchantTransactionId'] && !(typeof data['merchantTransactionId'] === 'string' || data['merchantTransactionId'] instanceof String)) {
            throw new Error("Expected the field `merchantTransactionId` to be a primitive type in the JSON string but got " + data['merchantTransactionId']);
        }
        // validate the optional field `encryptedPayload`
        if (data['encryptedPayload']) { // data not null
          CheckoutEncryptedPayload.validateJSON(data['encryptedPayload']);
        }
        // ensure the json data is a string
        if (data['correlationId'] && !(typeof data['correlationId'] === 'string' || data['correlationId'] instanceof String)) {
            throw new Error("Expected the field `correlationId` to be a primitive type in the JSON string but got " + data['correlationId']);
        }
        // ensure the json data is a string
        if (data['merchantCardId'] && !(typeof data['merchantCardId'] === 'string' || data['merchantCardId'] instanceof String)) {
            throw new Error("Expected the field `merchantCardId` to be a primitive type in the JSON string but got " + data['merchantCardId']);
        }
        // validate the optional field `assuranceData`
        if (data['assuranceData']) { // data not null
          AssuranceData.validateJSON(data['assuranceData']);
        }
        // validate the optional field `maskedCard`
        if (data['maskedCard']) { // data not null
          CheckoutMaskedCard.validateJSON(data['maskedCard']);
        }
        // validate the optional field `maskedConsumer`
        if (data['maskedConsumer']) { // data not null
          CheckoutMaskedConsumer.validateJSON(data['maskedConsumer']);
        }

        return true;
    }


}

CheckoutResponse.RequiredProperties = ["merchantTransactionId", "encryptedPayload", "correlationId"];

/**
 * Merchant Checkout Transaction Identifier which is returned in the checkoutResponse to either checkoutWithCard or checkoutWithNewCard JavaScript methods and links the client-side JavaScript calls and server-side API calls for a specific transaction. Optional for Subsequent Payment or Subsequent Split Shipment.
 * @member {String} merchantTransactionId
 */
CheckoutResponse.prototype['merchantTransactionId'] = undefined;

/**
 * @member {module:model/CheckoutEncryptedPayload} encryptedPayload
 */
CheckoutResponse.prototype['encryptedPayload'] = undefined;

/**
 * Correlation ID for this transaction. If available within the present checkout session (e.g. received in an earlier API response during the present session), then it must be provided, otherwise a new checkout session will be initiated.
 * @member {String} correlationId
 */
CheckoutResponse.prototype['correlationId'] = undefined;

/**
 * Merchant card identifer.
 * @member {String} merchantCardId
 */
CheckoutResponse.prototype['merchantCardId'] = undefined;

/**
 * @member {module:model/AssuranceData} assuranceData
 */
CheckoutResponse.prototype['assuranceData'] = undefined;

/**
 * @member {module:model/CheckoutMaskedCard} maskedCard
 */
CheckoutResponse.prototype['maskedCard'] = undefined;

/**
 * @member {module:model/CheckoutMaskedConsumer} maskedConsumer
 */
CheckoutResponse.prototype['maskedConsumer'] = undefined;






export default CheckoutResponse;

