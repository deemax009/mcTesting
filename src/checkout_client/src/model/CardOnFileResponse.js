/**
 * Checkout, Save Card On File, Delete Saved Card On File And Confirmations API
 *  Click to Pay checkout used by Merchants to receive payload data from the Click to Pay System and send the results of transaction authorisation back to the Click to Pay System. Endpoints are available to retrieve payload data and send the outcome of a payment or checkout. 
 *
 * The version of the OpenAPI document: 1.3.2
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CardOnFileMaskedCard from './CardOnFileMaskedCard';

/**
 * The CardOnFileResponse model module.
 * @module model/CardOnFileResponse
 * @version 1.3.2
 */
class CardOnFileResponse {
    /**
     * Constructs a new <code>CardOnFileResponse</code>.
     * @alias module:model/CardOnFileResponse
     * @param merchantCardId {String} Merchant Card ID returned by SRCI.
     * @param correlationId {String} Correlation ID for this transaction. If available within the present checkout session (e.g. received in an earlier API response during the present session), then it must be provided, otherwise a new checkout session will be initiated.
     */
    constructor(merchantCardId, correlationId) { 
        
        CardOnFileResponse.initialize(this, merchantCardId, correlationId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, merchantCardId, correlationId) { 
        obj['merchantCardId'] = merchantCardId;
        obj['correlationId'] = correlationId;
    }

    /**
     * Constructs a <code>CardOnFileResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CardOnFileResponse} obj Optional instance to populate.
     * @return {module:model/CardOnFileResponse} The populated <code>CardOnFileResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CardOnFileResponse();

            if (data.hasOwnProperty('merchantCardId')) {
                obj['merchantCardId'] = ApiClient.convertToType(data['merchantCardId'], 'String');
            }
            if (data.hasOwnProperty('correlationId')) {
                obj['correlationId'] = ApiClient.convertToType(data['correlationId'], 'String');
            }
            if (data.hasOwnProperty('maskedCard')) {
                obj['maskedCard'] = CardOnFileMaskedCard.constructFromObject(data['maskedCard']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CardOnFileResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CardOnFileResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CardOnFileResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['merchantCardId'] && !(typeof data['merchantCardId'] === 'string' || data['merchantCardId'] instanceof String)) {
            throw new Error("Expected the field `merchantCardId` to be a primitive type in the JSON string but got " + data['merchantCardId']);
        }
        // ensure the json data is a string
        if (data['correlationId'] && !(typeof data['correlationId'] === 'string' || data['correlationId'] instanceof String)) {
            throw new Error("Expected the field `correlationId` to be a primitive type in the JSON string but got " + data['correlationId']);
        }
        // validate the optional field `maskedCard`
        if (data['maskedCard']) { // data not null
          CardOnFileMaskedCard.validateJSON(data['maskedCard']);
        }

        return true;
    }


}

CardOnFileResponse.RequiredProperties = ["merchantCardId", "correlationId"];

/**
 * Merchant Card ID returned by SRCI.
 * @member {String} merchantCardId
 */
CardOnFileResponse.prototype['merchantCardId'] = undefined;

/**
 * Correlation ID for this transaction. If available within the present checkout session (e.g. received in an earlier API response during the present session), then it must be provided, otherwise a new checkout session will be initiated.
 * @member {String} correlationId
 */
CardOnFileResponse.prototype['correlationId'] = undefined;

/**
 * @member {module:model/CardOnFileMaskedCard} maskedCard
 */
CardOnFileResponse.prototype['maskedCard'] = undefined;






export default CardOnFileResponse;

