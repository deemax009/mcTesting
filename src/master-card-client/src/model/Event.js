/**
 * Mastercard Processing Core API
 * OpenAPI Specification for MP (Mastercard Processing) **Core**.  This documentation describes what functionalities are exposed by the MP's CMS (Card Management System) and are available to be used by the Issuer. The Issuer is the primary actor who executes the operations. Whenever the Issuer wishes to onboard its customers to the card program, they may use API to:  * create and manage **client**  * create and manage **contracts**, there are two types of contracts in the MP's CMS:    * **account contract**    * **card contract**  Later, during the card lifecycle, the Issuer can also manage other objects related to the client and the contract (account or card):  * manage **card plastic**  * create, manage and retrieve **additional addresses** (for the client and the contract)  * manage and retrieve **classifiers** (for the client and the contract)  * manage and retrieve **custom data** (for the client and the contract)  * manage and retrieve **parameters** (for the contract)  * set or retrieve **service limit tariff** (for the contract)  * post or retrieve **transactions** (for the contract)  * manage and retrieve **usage limits** (for the contract)  *** API uses RESTful protocol via HTTP to execute POST, PUT, GET and PATCH operations sent to given endpoints, e.g. `/clients`.  API uses JSON Web Encryption compact serialization for payload encryption for all API operations. *** External documentation:   * [ISO 4217 - Currency Codes](https://www.iso.org/iso-4217-currency-codes.html)   * [ISO 3166 - Country Codes](https://www.iso.org/iso-3166-country-codes.html)   * [Internet Engineering Task Force (IETF) BCP 47 standard](https://tools.ietf.org/html/bcp47#appendix-A)   * [JSON Web Encryption](https://developer.mastercard.com/platform/documentation/security-and-authentication/securing-sensitive-data-using-payload-encryption/#jwe-encryption)   * [Internet Engineering Task Force (IETF) RFC7232](https://datatracker.ietf.org/doc/html/rfc7232)   * [JSON Merge Patch](https://datatracker.ietf.org/doc/html/rfc7386) *** 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Event model module.
 * @module model/Event
 * @version 1.0.0
 */
class Event {
    /**
     * Constructs a new <code>Event</code>.
     * @alias module:model/Event
     * @param eventCode {String} Event code which will be executed in the MP's CMS. A list of possible codes will be defined by the MP during the project based on Issuer requirements. 
     */
    constructor(eventCode) { 
        
        Event.initialize(this, eventCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eventCode) { 
        obj['eventCode'] = eventCode;
    }

    /**
     * Constructs a <code>Event</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Event} obj Optional instance to populate.
     * @return {module:model/Event} The populated <code>Event</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Event();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('eventCode')) {
                obj['eventCode'] = ApiClient.convertToType(data['eventCode'], 'String');
            }
            if (data.hasOwnProperty('parameterString')) {
                obj['parameterString'] = ApiClient.convertToType(data['parameterString'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Event</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Event</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Event.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['eventCode'] && !(typeof data['eventCode'] === 'string' || data['eventCode'] instanceof String)) {
            throw new Error("Expected the field `eventCode` to be a primitive type in the JSON string but got " + data['eventCode']);
        }
        // ensure the json data is a string
        if (data['parameterString'] && !(typeof data['parameterString'] === 'string' || data['parameterString'] instanceof String)) {
            throw new Error("Expected the field `parameterString` to be a primitive type in the JSON string but got " + data['parameterString']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }

        return true;
    }


}

Event.RequiredProperties = ["eventCode"];

/**
 * The amount parameter for the event. Only one of 2 options: (`amount` and `currency`) or (`parameterString`) may be input in `POST /contracts/{contract_id}/events`.  The field can contain values up to 4 decimal places. A dot is used as a decimal separator. 
 * @member {Number} amount
 */
Event.prototype['amount'] = undefined;

/**
 * The currency parameter for the event (format according to ISO 4217, alphanumeric code). Only one of 2 options: (`amount` and `currency`) or (`parameterString`) may be input in `POST /contracts/{contract_id}/events`. 
 * @member {String} currency
 */
Event.prototype['currency'] = undefined;

/**
 * Event closing date (YYYY-MM-DD format). 
 * @member {Date} endDate
 */
Event.prototype['endDate'] = undefined;

/**
 * Event code which will be executed in the MP's CMS. A list of possible codes will be defined by the MP during the project based on Issuer requirements. 
 * @member {String} eventCode
 */
Event.prototype['eventCode'] = undefined;

/**
 * The parameter string for the event. Only one of 2 options: (`amount` and `currency`) or (`parameterString`) may be input in `POST /contracts/{contract_id}/events`. 
 * @member {String} parameterString
 */
Event.prototype['parameterString'] = undefined;

/**
 * Reason of the Event. 
 * @member {String} reason
 */
Event.prototype['reason'] = undefined;

/**
 * Event opening date (YYYY-MM-DD format). 
 * @member {Date} startDate
 */
Event.prototype['startDate'] = undefined;






export default Event;

