/**
 * Mastercard Processing Core API
 * OpenAPI Specification for MP (Mastercard Processing) **Core**.  This documentation describes what functionalities are exposed by the MP's CMS (Card Management System) and are available to be used by the Issuer. The Issuer is the primary actor who executes the operations. Whenever the Issuer wishes to onboard its customers to the card program, they may use API to:  * create and manage **client**  * create and manage **contracts**, there are two types of contracts in the MP's CMS:    * **account contract**    * **card contract**  Later, during the card lifecycle, the Issuer can also manage other objects related to the client and the contract (account or card):  * manage **card plastic**  * create, manage and retrieve **additional addresses** (for the client and the contract)  * manage and retrieve **classifiers** (for the client and the contract)  * manage and retrieve **custom data** (for the client and the contract)  * manage and retrieve **parameters** (for the contract)  * set or retrieve **service limit tariff** (for the contract)  * post or retrieve **transactions** (for the contract)  * manage and retrieve **usage limits** (for the contract)  *** API uses RESTful protocol via HTTP to execute POST, PUT, GET and PATCH operations sent to given endpoints, e.g. `/clients`.  API uses JSON Web Encryption compact serialization for payload encryption for all API operations. *** External documentation:   * [ISO 4217 - Currency Codes](https://www.iso.org/iso-4217-currency-codes.html)   * [ISO 3166 - Country Codes](https://www.iso.org/iso-3166-country-codes.html)   * [Internet Engineering Task Force (IETF) BCP 47 standard](https://tools.ietf.org/html/bcp47#appendix-A)   * [JSON Web Encryption](https://developer.mastercard.com/platform/documentation/security-and-authentication/securing-sensitive-data-using-payload-encryption/#jwe-encryption)   * [Internet Engineering Task Force (IETF) RFC7232](https://datatracker.ietf.org/doc/html/rfc7232)   * [JSON Merge Patch](https://datatracker.ietf.org/doc/html/rfc7386) *** 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TransactionFee model module.
 * @module model/TransactionFee
 * @version 1.0.0
 */
class TransactionFee {
    /**
     * Constructs a new <code>TransactionFee</code>.
     * @alias module:model/TransactionFee
     * @param feeCode {String} Fee code of the fee which should be applied for the transaction. The fee code which can be used must be configured in the MP's CMS. 
     * @param feeId {Number} Fee record id from the CMS system. 
     * @param transactionId {Number} Transaction record id to which the given fee was generated. 
     */
    constructor(feeCode, feeId, transactionId) { 
        
        TransactionFee.initialize(this, feeCode, feeId, transactionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, feeCode, feeId, transactionId) { 
        obj['feeCode'] = feeCode;
        obj['feeId'] = feeId;
        obj['transactionId'] = transactionId;
    }

    /**
     * Constructs a <code>TransactionFee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TransactionFee} obj Optional instance to populate.
     * @return {module:model/TransactionFee} The populated <code>TransactionFee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransactionFee();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('currencyNumericCode')) {
                obj['currencyNumericCode'] = ApiClient.convertToType(data['currencyNumericCode'], 'String');
            }
            if (data.hasOwnProperty('feeCode')) {
                obj['feeCode'] = ApiClient.convertToType(data['feeCode'], 'String');
            }
            if (data.hasOwnProperty('feeId')) {
                obj['feeId'] = ApiClient.convertToType(data['feeId'], 'Number');
            }
            if (data.hasOwnProperty('feeName')) {
                obj['feeName'] = ApiClient.convertToType(data['feeName'], 'String');
            }
            if (data.hasOwnProperty('transactionId')) {
                obj['transactionId'] = ApiClient.convertToType(data['transactionId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TransactionFee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TransactionFee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TransactionFee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['currencyNumericCode'] && !(typeof data['currencyNumericCode'] === 'string' || data['currencyNumericCode'] instanceof String)) {
            throw new Error("Expected the field `currencyNumericCode` to be a primitive type in the JSON string but got " + data['currencyNumericCode']);
        }
        // ensure the json data is a string
        if (data['feeCode'] && !(typeof data['feeCode'] === 'string' || data['feeCode'] instanceof String)) {
            throw new Error("Expected the field `feeCode` to be a primitive type in the JSON string but got " + data['feeCode']);
        }
        // ensure the json data is a string
        if (data['feeName'] && !(typeof data['feeName'] === 'string' || data['feeName'] instanceof String)) {
            throw new Error("Expected the field `feeName` to be a primitive type in the JSON string but got " + data['feeName']);
        }

        return true;
    }


}

TransactionFee.RequiredProperties = ["feeCode", "feeId", "transactionId"];

/**
 * Fee amount.  The field can contain values up to 4 decimal places. A dot is used as a decimal separator. 
 * @member {Number} amount
 */
TransactionFee.prototype['amount'] = undefined;

/**
 * Currency of the fee (format according to ISO 4217, alphanumeric code). 
 * @member {String} currency
 */
TransactionFee.prototype['currency'] = undefined;

/**
 * Currency of the fee (format according to ISO 4217, numeric code). 
 * @member {String} currencyNumericCode
 */
TransactionFee.prototype['currencyNumericCode'] = undefined;

/**
 * Fee code of the fee which should be applied for the transaction. The fee code which can be used must be configured in the MP's CMS. 
 * @member {String} feeCode
 */
TransactionFee.prototype['feeCode'] = undefined;

/**
 * Fee record id from the CMS system. 
 * @member {Number} feeId
 */
TransactionFee.prototype['feeId'] = undefined;

/**
 * Name of the fee from the CMS system. 
 * @member {String} feeName
 */
TransactionFee.prototype['feeName'] = undefined;

/**
 * Transaction record id to which the given fee was generated. 
 * @member {Number} transactionId
 */
TransactionFee.prototype['transactionId'] = undefined;






export default TransactionFee;

