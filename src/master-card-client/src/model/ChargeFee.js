/**
 * Mastercard Processing Core API
 * OpenAPI Specification for MP (Mastercard Processing) **Core**.  This documentation describes what functionalities are exposed by the MP's CMS (Card Management System) and are available to be used by the Issuer. The Issuer is the primary actor who executes the operations. Whenever the Issuer wishes to onboard its customers to the card program, they may use API to:  * create and manage **client**  * create and manage **contracts**, there are two types of contracts in the MP's CMS:    * **account contract**    * **card contract**  Later, during the card lifecycle, the Issuer can also manage other objects related to the client and the contract (account or card):  * manage **card plastic**  * create, manage and retrieve **additional addresses** (for the client and the contract)  * manage and retrieve **classifiers** (for the client and the contract)  * manage and retrieve **custom data** (for the client and the contract)  * manage and retrieve **parameters** (for the contract)  * set or retrieve **service limit tariff** (for the contract)  * post or retrieve **transactions** (for the contract)  * manage and retrieve **usage limits** (for the contract)  *** API uses RESTful protocol via HTTP to execute POST, PUT, GET and PATCH operations sent to given endpoints, e.g. `/clients`.  API uses JSON Web Encryption compact serialization for payload encryption for all API operations. *** External documentation:   * [ISO 4217 - Currency Codes](https://www.iso.org/iso-4217-currency-codes.html)   * [ISO 3166 - Country Codes](https://www.iso.org/iso-3166-country-codes.html)   * [Internet Engineering Task Force (IETF) BCP 47 standard](https://tools.ietf.org/html/bcp47#appendix-A)   * [JSON Web Encryption](https://developer.mastercard.com/platform/documentation/security-and-authentication/securing-sensitive-data-using-payload-encryption/#jwe-encryption)   * [Internet Engineering Task Force (IETF) RFC7232](https://datatracker.ietf.org/doc/html/rfc7232)   * [JSON Merge Patch](https://datatracker.ietf.org/doc/html/rfc7386) *** 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChargeFee model module.
 * @module model/ChargeFee
 * @version 1.0.0
 */
class ChargeFee {
    /**
     * Constructs a new <code>ChargeFee</code>.
     * @alias module:model/ChargeFee
     * @param feeTypeId {String} Identifier of the fee type in the MP's CMS system. 
     * @param uniqueReferenceNumber {String} Unique reference number for fee transaction record. If filled system will check the uniqueness of this value. It will not be possible to create two records with the same value. Exported as SRN (Source Reference Number). 
     */
    constructor(feeTypeId, uniqueReferenceNumber) { 
        
        ChargeFee.initialize(this, feeTypeId, uniqueReferenceNumber);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, feeTypeId, uniqueReferenceNumber) { 
        obj['feeTypeId'] = feeTypeId;
        obj['uniqueReferenceNumber'] = uniqueReferenceNumber;
    }

    /**
     * Constructs a <code>ChargeFee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChargeFee} obj Optional instance to populate.
     * @return {module:model/ChargeFee} The populated <code>ChargeFee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChargeFee();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('feeTypeId')) {
                obj['feeTypeId'] = ApiClient.convertToType(data['feeTypeId'], 'String');
            }
            if (data.hasOwnProperty('postingDate')) {
                obj['postingDate'] = ApiClient.convertToType(data['postingDate'], 'Date');
            }
            if (data.hasOwnProperty('uniqueReferenceNumber')) {
                obj['uniqueReferenceNumber'] = ApiClient.convertToType(data['uniqueReferenceNumber'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChargeFee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChargeFee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChargeFee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['feeTypeId'] && !(typeof data['feeTypeId'] === 'string' || data['feeTypeId'] instanceof String)) {
            throw new Error("Expected the field `feeTypeId` to be a primitive type in the JSON string but got " + data['feeTypeId']);
        }
        // ensure the json data is a string
        if (data['uniqueReferenceNumber'] && !(typeof data['uniqueReferenceNumber'] === 'string' || data['uniqueReferenceNumber'] instanceof String)) {
            throw new Error("Expected the field `uniqueReferenceNumber` to be a primitive type in the JSON string but got " + data['uniqueReferenceNumber']);
        }

        return true;
    }


}

ChargeFee.RequiredProperties = ["feeTypeId", "uniqueReferenceNumber"];

/**
 * Amount to charge a fee. If the field is not passed in the request, the amount is taken from the tariff level.  The field can contain values up to 4 decimal places. A dot is used as a decimal separator. 
 * @member {Number} amount
 */
ChargeFee.prototype['amount'] = undefined;

/**
 * Transaction currency (format according to ISO 4217, alphanumeric code). 
 * @member {String} currency
 */
ChargeFee.prototype['currency'] = undefined;

/**
 * Identifier of the fee type in the MP's CMS system. 
 * @member {String} feeTypeId
 */
ChargeFee.prototype['feeTypeId'] = undefined;

/**
 * Posting date (YYYY-MM-DD format). If empty, current banking date will be used. If future date, fee record will stay in `Waiting` status and will be posted only when banking date reaches defined `postingDate`. Past date not recommended. 
 * @member {Date} postingDate
 */
ChargeFee.prototype['postingDate'] = undefined;

/**
 * Unique reference number for fee transaction record. If filled system will check the uniqueness of this value. It will not be possible to create two records with the same value. Exported as SRN (Source Reference Number). 
 * @member {String} uniqueReferenceNumber
 */
ChargeFee.prototype['uniqueReferenceNumber'] = undefined;






export default ChargeFee;

