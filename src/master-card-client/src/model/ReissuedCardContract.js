/**
 * Mastercard Processing Core API
 * OpenAPI Specification for MP (Mastercard Processing) **Core**.  This documentation describes what functionalities are exposed by the MP's CMS (Card Management System) and are available to be used by the Issuer. The Issuer is the primary actor who executes the operations. Whenever the Issuer wishes to onboard its customers to the card program, they may use API to:  * create and manage **client**  * create and manage **contracts**, there are two types of contracts in the MP's CMS:    * **account contract**    * **card contract**  Later, during the card lifecycle, the Issuer can also manage other objects related to the client and the contract (account or card):  * manage **card plastic**  * create, manage and retrieve **additional addresses** (for the client and the contract)  * manage and retrieve **classifiers** (for the client and the contract)  * manage and retrieve **custom data** (for the client and the contract)  * manage and retrieve **parameters** (for the contract)  * set or retrieve **service limit tariff** (for the contract)  * post or retrieve **transactions** (for the contract)  * manage and retrieve **usage limits** (for the contract)  *** API uses RESTful protocol via HTTP to execute POST, PUT, GET and PATCH operations sent to given endpoints, e.g. `/clients`.  API uses JSON Web Encryption compact serialization for payload encryption for all API operations. *** External documentation:   * [ISO 4217 - Currency Codes](https://www.iso.org/iso-4217-currency-codes.html)   * [ISO 3166 - Country Codes](https://www.iso.org/iso-3166-country-codes.html)   * [Internet Engineering Task Force (IETF) BCP 47 standard](https://tools.ietf.org/html/bcp47#appendix-A)   * [JSON Web Encryption](https://developer.mastercard.com/platform/documentation/security-and-authentication/securing-sensitive-data-using-payload-encryption/#jwe-encryption)   * [Internet Engineering Task Force (IETF) RFC7232](https://datatracker.ietf.org/doc/html/rfc7232)   * [JSON Merge Patch](https://datatracker.ietf.org/doc/html/rfc7386) *** 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReissuedCardContract model module.
 * @module model/ReissuedCardContract
 * @version 1.0.0
 */
class ReissuedCardContract {
    /**
     * Constructs a new <code>ReissuedCardContract</code>.
     * @alias module:model/ReissuedCardContract
     * @param newCardContractId {Number} Unique technical card contract identifier generated by the MP's CMS. The identifier is generated when card contract creation finishes successfully and is returned in a card contract creation response (`POST /cards`). 
     * @param newCardContractNumber {String} The card contract number represents the Primary Account Number (PAN). A PAN usually consists of 16 digits:   * The first six digits are the Bank Identification Number (BIN): a unique number within the payment organization (Mastercard, VISA)   * The following nine digits are the contract identification number, which can be generated randomly   * The last digit is a Luhn check digit.  The Issuer may enter a PAN number generated by themselves or leave the field empty: in such case, the MP's CMS will generate a PAN number on behalf of the Issuer.  To generate a PAN, the MP's CMS must be configured with BIN ranges for a given card product. 
     * @param newCardExpiryDate {String} New card expiry date (YYMM format). 
     * @param newSequenceNumber {String} The sequence number of a `Plastic`, created under the given card contract. Every new `Plastic` created has the next number in a sequence following previous card plastics. 
     * @param plasticId {Number} Technical identifier of a card plastic record in the MP's CMS. 
     */
    constructor(newCardContractId, newCardContractNumber, newCardExpiryDate, newSequenceNumber, plasticId) { 
        
        ReissuedCardContract.initialize(this, newCardContractId, newCardContractNumber, newCardExpiryDate, newSequenceNumber, plasticId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, newCardContractId, newCardContractNumber, newCardExpiryDate, newSequenceNumber, plasticId) { 
        obj['newCardContractId'] = newCardContractId;
        obj['newCardContractNumber'] = newCardContractNumber;
        obj['newCardExpiryDate'] = newCardExpiryDate;
        obj['newSequenceNumber'] = newSequenceNumber;
        obj['plasticId'] = plasticId;
    }

    /**
     * Constructs a <code>ReissuedCardContract</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReissuedCardContract} obj Optional instance to populate.
     * @return {module:model/ReissuedCardContract} The populated <code>ReissuedCardContract</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReissuedCardContract();

            if (data.hasOwnProperty('newCardContractId')) {
                obj['newCardContractId'] = ApiClient.convertToType(data['newCardContractId'], 'Number');
            }
            if (data.hasOwnProperty('newCardContractNumber')) {
                obj['newCardContractNumber'] = ApiClient.convertToType(data['newCardContractNumber'], 'String');
            }
            if (data.hasOwnProperty('newCardExpiryDate')) {
                obj['newCardExpiryDate'] = ApiClient.convertToType(data['newCardExpiryDate'], 'String');
            }
            if (data.hasOwnProperty('newSequenceNumber')) {
                obj['newSequenceNumber'] = ApiClient.convertToType(data['newSequenceNumber'], 'String');
            }
            if (data.hasOwnProperty('plasticId')) {
                obj['plasticId'] = ApiClient.convertToType(data['plasticId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReissuedCardContract</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReissuedCardContract</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReissuedCardContract.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['newCardContractNumber'] && !(typeof data['newCardContractNumber'] === 'string' || data['newCardContractNumber'] instanceof String)) {
            throw new Error("Expected the field `newCardContractNumber` to be a primitive type in the JSON string but got " + data['newCardContractNumber']);
        }
        // ensure the json data is a string
        if (data['newCardExpiryDate'] && !(typeof data['newCardExpiryDate'] === 'string' || data['newCardExpiryDate'] instanceof String)) {
            throw new Error("Expected the field `newCardExpiryDate` to be a primitive type in the JSON string but got " + data['newCardExpiryDate']);
        }
        // ensure the json data is a string
        if (data['newSequenceNumber'] && !(typeof data['newSequenceNumber'] === 'string' || data['newSequenceNumber'] instanceof String)) {
            throw new Error("Expected the field `newSequenceNumber` to be a primitive type in the JSON string but got " + data['newSequenceNumber']);
        }

        return true;
    }


}

ReissuedCardContract.RequiredProperties = ["newCardContractId", "newCardContractNumber", "newCardExpiryDate", "newSequenceNumber", "plasticId"];

/**
 * Unique technical card contract identifier generated by the MP's CMS. The identifier is generated when card contract creation finishes successfully and is returned in a card contract creation response (`POST /cards`). 
 * @member {Number} newCardContractId
 */
ReissuedCardContract.prototype['newCardContractId'] = undefined;

/**
 * The card contract number represents the Primary Account Number (PAN). A PAN usually consists of 16 digits:   * The first six digits are the Bank Identification Number (BIN): a unique number within the payment organization (Mastercard, VISA)   * The following nine digits are the contract identification number, which can be generated randomly   * The last digit is a Luhn check digit.  The Issuer may enter a PAN number generated by themselves or leave the field empty: in such case, the MP's CMS will generate a PAN number on behalf of the Issuer.  To generate a PAN, the MP's CMS must be configured with BIN ranges for a given card product. 
 * @member {String} newCardContractNumber
 */
ReissuedCardContract.prototype['newCardContractNumber'] = undefined;

/**
 * New card expiry date (YYMM format). 
 * @member {String} newCardExpiryDate
 */
ReissuedCardContract.prototype['newCardExpiryDate'] = undefined;

/**
 * The sequence number of a `Plastic`, created under the given card contract. Every new `Plastic` created has the next number in a sequence following previous card plastics. 
 * @member {String} newSequenceNumber
 */
ReissuedCardContract.prototype['newSequenceNumber'] = undefined;

/**
 * Technical identifier of a card plastic record in the MP's CMS. 
 * @member {Number} plasticId
 */
ReissuedCardContract.prototype['plasticId'] = undefined;






export default ReissuedCardContract;

